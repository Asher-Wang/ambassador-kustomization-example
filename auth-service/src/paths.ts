import Path from 'path';

export const GLOBAL_CONFIG_PATH = '/etc/mock/auth-service.json';

export const PROJECT_DIR = Path.join(__dirname, '..');
export const PROJECT_CONFIG_PATH = Path.join(PROJECT_DIR, 'auth-service.json');
